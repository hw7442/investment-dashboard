name: Update Heartbeat

on:
  schedule:
    - cron: '30 10 * * 1-5'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 安装依赖
        run: pip install yfinance
      - name: 执行更新脚本
        run: python update_heartbeat.py
      - name: 提交更新
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add HEARTBEAT.md
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 自动更新投资心跳日报"
          git push
